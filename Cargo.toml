[package]
name = "git-todo"
version = "0.1.0"
edition = "2021"

[dependencies]
im = {version = "15.1.0"}
floem = { git = "https://github.com/lapce/floem.git", features = ["rfd-async-std"] }
strum = { version = "0.25.0", features = ["derive"] }
color = "0.2.3"
rusqlite = { version = "0.34.0", features = ["bundled"] }

[features]
vello = ["floem/vello"]
